# Terraform local state/provider cache -- never commit, contains resource details.
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/*.tfplan
terraform/terraform.tfvars

# Generated by terraform apply -- SSH key and inventory are specific to a live deployment.
ansible/*.pem
ansible/inventory.ini
